const strictEquals = (value) => {
    const strictEquals = (type) => {

        if (value == value, type == type) {
            return true
        }
        else {
            return false
        }

    }
}
module.exports = strictEquals